#include <iostream>
#include <cstdlib>
#include <ctime>
#include "Array.h"

/**********************************
 * Question 1: Swap Counts
 * a. 4
 * b. 6
 * c. 6 - does each swap that occurs in the innermost iteration count as an individual swap or a all swaps to its proper place count as one swap?
 **********************************/

template <typename T>
bool Monotonic(const Array<T>& data)
{
    return false;
}

int main()
{
    srand(time(NULL));

    return 0; 
}
